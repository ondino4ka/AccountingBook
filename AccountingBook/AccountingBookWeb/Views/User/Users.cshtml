@model IEnumerable<AccountingBookWeb.Models.UserViewModel>

@if (Model != null)
{
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.ActionLink("Edit Information", "EditUserInformation", new { id = item.Id }) |
                    @Html.ActionLink("Edit password", "AddEditUser", new { id = item.Id }) |              
                    @Html.ActionLink("Delete", "DeleteUser", new { id = item.Id })
                </td>
            </tr>
        }

    </table>
}
else
{
    @ViewBag.Error;
}